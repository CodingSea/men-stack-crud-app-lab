<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="main.css">
</head>
<body>
    <h1>All Foods</h1>
    <a href="/food/new"><button>Add a Dish</button></a>
    <% food.forEach((dish) => { %>
        <div class="dish">
            <p>Name: <%= dish.name %></p>
            <p>Description: <%= dish.description %></p>
            <p>Country of Origin: <%= dish.countryOfOrigin %></p>
            <img src="<%= dish.image %>">
            <a href="/food/<%= dish.id %>/view"><button>View</button></a>
            <a href="/food/<%= dish.id %>/edit"><button>Edit</button></a>
            <form method="post" action="/food/<%= dish.id %>?_method=DELETE">
                <button>Delete</button>
            </form>
        </div>
    <% }); %>
</body>
</html>